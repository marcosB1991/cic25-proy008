drop table if exists libro cascade
drop sequence if exists libro_seq
create sequence libro_seq start with 1 increment by 50
create table libro (
    id bigint generated by default as identity,
    autor varchar(255),
    año_de_publicacion integer not null,
    nombre_libro varchar(255),
    primary key (id)
)